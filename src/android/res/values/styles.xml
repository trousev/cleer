<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BaseTheme" parent="@android:style/Theme.NoTitleBar">

        <!-- Костыли и фичи для версий ниже 3.0 писать здесь -->
        <item name="android:thumb">@drawable/thumb</item>
        <item name="android:progressDrawable">@drawable/seek_bar_drawable</item>
    </style>

    <style name="CleerTheme" parent="BaseTheme">
        <!-- Общие костыли и фичи для всех версий -->
    </style>

   <!-- Попытка сварганить няшный RatingBar, спасибо http://stackoverflow.com/questions/5800657/ -->
   <style name="foodRatingBar" parent="@android:style/Widget.RatingBar">
       <item name="android:progressDrawable">@drawable/food_rating_bar_full</item>
       <item name="android:minHeight">23dip</item>
       <item name="android:maxHeight">25dip</item>
   </style>
    
</resources>