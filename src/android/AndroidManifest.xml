<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="pro.trousev.cleer.android.userInterface"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" android:icon="@drawable/application_icon">
        <activity android:name="MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name="pro.trousev.cleer.android.service.AndroidCleerService">
            <intent-filter>
                <action android:name="pro.trousev.cleer.android.service.AndroidCleerService"></action>
            </intent-filter>
        </service>
        
    </application>
</manifest>
